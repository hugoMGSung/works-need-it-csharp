<Window x:Class="WpfCreateWinApp.Views.SubView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCreateWinApp.Views"
        mc:Ignorable="d"
        Title="SubView"
        Height="180"
        Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               Margin="5,8"
               Content="윈도우 속성 : "
               x:Name="LblWindowStyle">
        </Label>

        <Label Grid.Row="1"
               Grid.Column="0"
               Margin="5,8"
               Content="시작위치 속성 : "
               x:Name="LblStartLocation">
        </Label>

        <Label Grid.Row="2"
               Grid.Column="0"
               Margin="5,8"
               Content="모달 속성 : "
               x:Name="LblModalType">
        </Label>

        <Button x:Name="BtnConfirm"
                Content="Confirm"
                Grid.Row="2"
                Grid.Column="1"
                Margin="5"
                Click="BtnConfirm_Click" />
    </Grid>
</Window>
